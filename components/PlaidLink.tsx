/**
 * PlaidLink — stubbed out. ISTBANK uses local mock accounts.
 * This file exists only to prevent import errors from any remaining references.
 */
const PlaidLink = ({ user, variant }: any) => null;
export default PlaidLink;
