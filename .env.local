# ISTBANK - Local Environment
# No external API keys needed — all services are mocked locally

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Sentry (disabled for local demo)
SENTRY_AUTH_TOKEN=disabled
